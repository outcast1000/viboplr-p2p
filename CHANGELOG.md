# Changelog

## v0.2.0
- Moved the plugin (JS UI shell) to its own repository with in-app auto-update.
- Note: the P2P engine remains in the host app's Rust + the viboplr-relay repo;
  releases here change only the UI/orchestration. minAppVersion gates updates.
